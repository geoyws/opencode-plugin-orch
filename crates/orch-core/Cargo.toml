[package]
name = "orch-core"
version = "0.1.0"
edition = "2024"
license = "MIT"
description = "Native deterministic kernel for opencode-plugin-orch"

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
