// TUI plugin entry point — Tier 2 (stretch goal)
// This would export a TuiPluginModule for persistent status bar rendering.
// For now, all TUI feedback is handled via:
//   - orch_status tool output (powerline format)
//   - client.tui.showToast() for state change notifications
//
// A full TUI plugin would use @opentui/core and @opentui/solid to render
// a persistent bottom bar in the opencode TUI.

export default {};
